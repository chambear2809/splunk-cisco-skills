interface:
  display_name: "Splunk DB Connect Setup"
  short_description: "Render and validate Splunk DB Connect workflows"
  default_prompt: "Use $splunk-db-connect-setup to render, preflight, validate, and hand off Splunk DB Connect JDBC ingestion, lookup, enrichment, export, and Federated Search assets."
