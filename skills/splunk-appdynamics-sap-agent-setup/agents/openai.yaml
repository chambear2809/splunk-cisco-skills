interface:
  display_name: "Splunk AppDynamics SAP Agent Setup"
  short_description: "Render AppDynamics SAP Agent, ABAP, SDK, and collector runbooks"
  default_prompt: "Use $splunk-appdynamics-sap-agent-setup to render AppDynamics SAP Agent, ABAP, SDK, and collector runbooks."
