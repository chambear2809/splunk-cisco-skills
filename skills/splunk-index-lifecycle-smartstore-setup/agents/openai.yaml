interface:
  display_name: "Splunk Index Lifecycle / SmartStore Setup"
  short_description: "Manage index lifecycle and SmartStore assets"
  default_prompt: >-
    Use $splunk-index-lifecycle-smartstore-setup to inventory Splunk indexes,
    plan retention/archive/restore actions, and render gated SmartStore,
    disable, delete, or clean-data lifecycle assets.
