interface:
  display_name: "Splunk Ingest Processor Setup"
  short_description: "Render and validate Splunk Ingest Processor setup plans"
  default_prompt: "Use $splunk-ingest-processor-setup to render and validate Splunk Ingest Processor setup plans."
