interface:
  display_name: "Splunk Add-on for Box Setup"
  short_description: "Install and validate the Splunk Add-on for Box"
  default_prompt: "Use $splunk-box-ta-setup to render, install, and validate Splunk_TA_box with safe OAuth account handoffs."
