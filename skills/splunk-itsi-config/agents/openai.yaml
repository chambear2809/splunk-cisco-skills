interface:
  display_name: "Splunk ITSI Config"
  short_description: "Preview, apply, and validate native ITSI objects and selected ITSI content packs"
  default_prompt: "Use $splunk-itsi-config to preview, apply, or validate native ITSI objects and selected ITSI content packs from specs under skills/splunk-itsi-config/ or other repo-local YAML files."
