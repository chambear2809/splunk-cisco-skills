interface:
  display_name: "Splunk Observability Database Monitoring"
  short_description: "Deploy and verify production Splunk DBMon"
  default_prompt: "Use $splunk-observability-database-monitoring-setup to render and validate Splunk Observability Cloud Database Monitoring for Microsoft SQL Server, MySQL 5.7+, MariaDB 10.5+, Oracle Database, or provider-paired PostgreSQL, preserving the documented explain-plan gaps for MySQL 5.7 and MariaDB. Safely apply/roll back Kubernetes or Linux, or emit the reviewed fail-closed Windows handoff. Pin the audited 0.158.0 collector/chart, require reviewed sizing and secure datasource transport, keep secrets in protected files or Kubernetes Secrets, use isolated metrics/dbmon* and logs/dbmon* pipelines with signalfx/dbmon plus otlp_http/dbmon, bind Kubernetes actions to a reviewed context, declare exactly one scrape owner, prevent duplicate receivers, preserve resumable transactional rollback state, and require a positive identity-filtered API probe plus applicable Infrastructure, DBMon UI, query, APM-correlation, and requested AI feature evidence before declaring success."
